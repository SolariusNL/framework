default:
  image: node:latest

stages:
  - build-status

build status:
  stage: build-status
  script:
    - yarn
    - yarn build
  cache:
    paths:
      - node_modules
      - .next/cache/
    key: ${CI_COMMIT_REF_SLUG}